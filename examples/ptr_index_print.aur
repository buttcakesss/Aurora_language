fn main() -> i64 {
  let p: ptr<i64> = malloc(40);
  
  p[0] = 10;
  p[1] = 20;
  p[2] = 30;
  p[3] = 40;
  p[4] = 50;
  
  print_i64(p[0]);
  print_i64(p[1]);
  print_i64(p[2]);
  print_i64(p[3]);
  print_i64(p[4]);
  
  let sum: i64 = p[0] + p[1] + p[2] + p[3] + p[4];
  print_i64(sum);
  
  free(p);
  return sum;
}